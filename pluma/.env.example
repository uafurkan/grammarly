# Copy to .env.local and fill in your own keys (all optional).
# .env.local is gitignored — keys never enter the repository.
# Engines without a key can still be connected from the app UI.
VITE_BRAVE_KEY=
VITE_SERPER_KEY=
VITE_BING_KEY=
VITE_GOOGLE_KEY=
VITE_GOOGLE_CX=
